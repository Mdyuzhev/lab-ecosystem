# TASK: Заменить emoji на SVG иконки

## Цель

Заменить все текстовые emoji на красивые SVG иконки из Lucide React для консистентного и профессионального вида.

## Проект уже использует Lucide

В проекте установлен `lucide-react`. Импортируй нужные иконки:

```jsx
import { Building2, GraduationCap, Scale, Brain, Monitor, Bot, Package, Wrench, Briefcase, FileText, Plug, DollarSign } from 'lucide-react'
```

## Маппинг emoji → Lucide иконки

| Emoji | Заменить на | Lucide import |
|-------|-------------|---------------|
| 🏢 | Building2 | `Building2` |
| 🎓 | GraduationCap | `GraduationCap` |
| ⚖️ | Scale | `Scale` |
| 🧠 | Brain | `Brain` |
| 💻 | Monitor | `Monitor` |
| 🤖 | Bot | `Bot` |
| 📦 | Package | `Package` |
| 🛠️ | Wrench | `Wrench` |
| 💼 | Briefcase | `Briefcase` |
| 📜 | FileText | `FileText` |
| 🔌 | Plug | `Plug` |
| 💰 | DollarSign | `DollarSign` |
| 🚀 | Rocket | `Rocket` |
| ✅ | CheckCircle | `CheckCircle` |
| 📅 | Calendar | `Calendar` |
| 🎯 | Target | `Target` |

## Как заменять

### До:
```jsx
<span className="text-xl">🏢</span>
```

### После:
```jsx
<Building2 className="w-5 h-5 text-blue-400" />
```

## Файлы для обработки

1. `src/pages/Ecosystem.jsx` — основной файл с emoji
2. `src/pages/Economics.jsx` — если есть emoji
3. `src/pages/Roadmap.jsx` — если есть emoji
4. `src/pages/Labs.jsx` — уже использует Lucide, проверить на оставшиеся emoji
5. `src/components/*.jsx` — проверить все компоненты

## Шаги

### Шаг 1 — Найти все emoji

```bash
grep -rn "[\x{1F300}-\x{1F9FF}]" src/ --include="*.jsx"
```

Или вручную поиском по файлам.

### Шаг 2 — Обновить импорты

Добавить нужные иконки в import в каждом файле.

### Шаг 3 — Заменить emoji на компоненты

Заменять по одному, сохраняя размеры и цвета.

### Шаг 4 — Проверить отображение

```bash
npm run dev
```

Открыть http://localhost:3000 и проверить что все иконки отображаются корректно.

### Шаг 5 — Коммит

```bash
git add -A
git commit -m "feat(ui): replace emoji with Lucide SVG icons"
git push
```

## Стиль иконок

Для консистентности использовать:
- Размер: `w-5 h-5` или `w-6 h-6` для крупных
- Цвет: наследовать от контекста или явно указать (`text-blue-400`, `text-amber-400` и т.д.)
- В кругах/квадратах: `w-6 h-6` внутри контейнера

## Пример полной замены

```jsx
// До
<div className="w-10 h-10 rounded-lg bg-blue-500/20 flex items-center justify-center">
  <span className="text-xl">🏢</span>
</div>

// После
<div className="w-10 h-10 rounded-lg bg-blue-500/20 flex items-center justify-center">
  <Building2 className="w-5 h-5 text-blue-400" />
</div>
```

## Результат

- Все emoji заменены на Lucide SVG
- Иконки масштабируемые и чёткие на любых экранах
- Консистентный визуальный стиль
- Деплой обновится автоматически после пуша

---

*Работать автономно, заменять все найденные emoji*
