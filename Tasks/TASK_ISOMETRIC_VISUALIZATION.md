# Задача: Изометрическая 2.5D визуализация экосистемы лабораторий

> Проект: `E:\Politech\lab-ecosystem`  
> Приоритет: Высокий  
> Оценка: 3-4 часа  
> Ветка: `feat/isometric-visualization`

---

## Контекст

Сайт-презентация экосистемы R&D лабораторий "Политех × Ростелеком". Текущая визуализация — плоские 2D карточки и блок-схемы. Нужно переработать главную визуальную секцию в **изометрический (2.5D) формат** — как в стратегических играх (SimCity, Factorio). Целевая аудитория — руководство, партнёры, инвесторы. Критично: ясность, "деньги видны", профессионализм.

**Стек проекта:** React 18, Vite, Tailwind CSS, Framer Motion, Lucide React, Lenis (smooth scroll). Деплой — GitHub Pages.

---

## ШАГ 0: Подготовка

```bash
cd E:\Politech\lab-ecosystem
git checkout -b feat/isometric-visualization
npm install
npm run dev
```

Открой `http://localhost:3000`, сделай скриншот текущего состояния для сравнения.

---

## ШАГ 1: Создать компонент IsometricEcosystem

Путь: `src/components/IsometricEcosystem.jsx`

### 1.1 Изометрическая сетка

Базовая CSS-трансформация для изометрии:

```css
.isometric-container {
  transform: rotateX(55deg) rotateZ(-45deg);
  transform-style: preserve-3d;
}
```

Но мы используем Tailwind + inline styles. Контейнер должен иметь `perspective: 1200px` на родителе для глубины.

### 1.2 Три "здания" лабораторий

Каждая лаборатория — изометрический блок (параллелепипед) с тремя видимыми гранями: верхняя, левая боковая, правая боковая. Реализация через CSS `transform` на трёх `div`-ах.

**Структура блока:**

```
┌─────────────┐  ← верхняя грань (светлее)
│╲            │
│  ╲──────────│  ← правая боковая (темнее)
│  │          │
│  │  КОНТЕНТ │
│  │          │
└──│──────────┘
   └──────────   ← левая боковая (средний тон)
```

**Три лаборатории с цветовой схемой:**

| Лаборатория | Основной цвет | CSS-класс | Иконка |
|---|---|---|---|
| ИИ "Фабрика инструментов" | Emerald (#10B981) | `emerald` | Brain / Cpu |
| ПО "Быстрые прототипы" | Blue (#3B82F6) | `blue` | Code / Monitor |
| Робототехника "Железо + интеллект" | Purple (#8B5CF6) | `purple` | Cog / Bot |

### 1.3 Данные для каждого здания

```javascript
const labs = [
  {
    id: 'ai',
    title: 'Лаборатория ИИ',
    subtitle: '«Фабрика инструментов»',
    color: 'emerald',
    team: { rt: '1 Tech Lead', uni: '1 Научрук + 3 студента' },
    output: 'Модули, библиотеки, методики',
    revenue: '5-15 млн₽/год',
    icon: 'Brain', // из lucide-react
    height: 180, // высота здания в px — варьировать для визуальной иерархии
  },
  {
    id: 'software',
    title: 'Лаборатория ПО',
    subtitle: '«Быстрые прототипы»',
    color: 'blue',
    team: { rt: '2 Tech Lead', uni: '2 Научрука + 6 студентов' },
    output: 'MVP за 2-4 мес, production решения',
    revenue: '20-50 млн₽/год',
    icon: 'Code',
    height: 220, // самое высокое здание — ключевая лаборатория
  },
  {
    id: 'robotics',
    title: 'Лаборатория Робототехники',
    subtitle: '«Железо + интеллект»',
    color: 'purple',
    team: { rt: '1 Tech Lead + 1 инженер', uni: '1 Научрук + 4 студента' },
    output: 'Прототипы роботов',
    revenue: '10-30 млн₽/год',
    icon: 'Cog',
    height: 200,
  },
];
```

### 1.4 Анимированные потоки между зданиями

Между зданиями — SVG-линии с анимированными "частицами" (маленькие кружки, двигающиеся по path). Три типа потоков с разными цветами:

| Поток | Цвет | Направление |
|---|---|---|
| Данные / инструменты | Cyan (#06B6D4) | ИИ → ПО, ИИ → Робототехника |
| Запросы / задачи | Amber (#F59E0B) | ПО → ИИ, Робототехника → ИИ |
| Деньги / ROI | Green (#22C55E) | От каждого здания наружу (вверх) |

Реализация частиц:

```jsx
// SVG circle с animateMotion по path
<circle r="3" fill="#06B6D4" opacity="0.8">
  <animateMotion dur="3s" repeatCount="indefinite" path="M0,0 C50,-30 100,0 150,20" />
</circle>
```

Или через Framer Motion для лучшего контроля — `motion.circle` с `animate` по координатам.

### 1.5 Управляющий комитет сверху

Над зданиями — полупрозрачная "облачная" платформа с текстом "Управляющий комитет". Связана пунктирными линиями со всеми тремя зданиями. Содержит:
- Ростелеком (70%) — синий бейдж
- Политех (30%) — фиолетовый бейдж

### 1.6 Участники по бокам

Слева — иконки участников от Ростелекома (синие аватары), справа — от вуза (фиолетовые). Тонкие линии связи к соответствующим зданиям. При hover — tooltip с ролью.

---

## ШАГ 2: Интерактивность

### 2.1 Hover-эффект на здании

При наведении на здание:
1. Здание "поднимается" на 10px вверх (`translateY(-10px)`) с `transition: 0.3s ease`
2. Тень усиливается
3. Появляется развёрнутая панель справа/снизу от здания с деталями:
   - Команда (от РТ и от вуза)
   - Ключевые метрики
   - ROI / выручка (крупным шрифтом, зелёным)
4. Потоки, связанные с этим зданием, становятся ярче, остальные — приглушаются

### 2.2 State management

```javascript
const [activeLab, setActiveLab] = useState(null);
// null = все видны, 'ai' | 'software' | 'robotics' = фокус на одном
```

### 2.3 Анимация при появлении в viewport

Использовать `useInView` из Framer Motion. Здания появляются последовательно с задержкой 0.2s между ними, "вырастая" снизу вверх (scaleY от 0 до 1 с transform-origin: bottom).

---

## ШАГ 3: Изометрический CSS-подход (без Three.js)

**ВАЖНО:** НЕ использовать Three.js / React Three Fiber. Вся визуализация — через CSS transforms + SVG + Framer Motion. Это обеспечивает:
- Нулевой дополнительный бандл
- Работу на любых устройствах включая слабые планшеты
- Доступность текста для screen readers

### 3.1 CSS для изометрического блока

```css
/* В index.css или как Tailwind @layer */
.iso-building {
  position: relative;
  transform-style: preserve-3d;
  transition: transform 0.3s ease, filter 0.3s ease;
}

.iso-building:hover {
  transform: translateY(-10px);
}

/* Верхняя грань */
.iso-top {
  position: absolute;
  width: 160px;
  height: 160px;
  transform: rotateX(60deg) rotateZ(45deg);
  transform-origin: center;
}

/* Левая боковая грань */
.iso-left {
  position: absolute;
  width: 160px;
  transform: skewY(-30deg);
  transform-origin: top left;
}

/* Правая боковая грань */
.iso-right {
  position: absolute;
  width: 160px;
  transform: skewY(30deg);
  transform-origin: top right;
}
```

**Альтернативный (рекомендуемый) подход — SVG изометрия:**

Если CSS transforms дают артефакты — реализовать здания как SVG `<polygon>` с изометрическими координатами. Это даёт pixel-perfect контроль и проще для анимированных путей между зданиями.

```jsx
// Изометрические координаты здания
// Верхняя грань — ромб
<polygon points="100,20 200,70 100,120 0,70" fill="emerald-400" />
// Левая грань
<polygon points="0,70 100,120 100,220 0,170" fill="emerald-600" />  
// Правая грань
<polygon points="100,120 200,70 200,170 100,220" fill="emerald-500" />
```

### 3.2 Выбор подхода

Попробуй **сначала SVG-подход** — он надёжнее для изометрии. Если результат устраивает, не переключайся на CSS. SVG даёт:
- Точные координаты без субпиксельных артефактов
- Простую анимацию путей (частицы по path)
- Единый контейнер для зданий и потоков

---

## ШАГ 4: Адаптивность

### Брейкпоинты:

| Размер | Поведение |
|---|---|
| `lg` (1024px+) | Полная изометрическая сцена |
| `md` (768-1023px) | Изометрия с уменьшенным масштабом (scale 0.75) |
| `sm` (<768px) | Fallback на вертикальные карточки (НЕ изометрия) — три карточки друг под другом с упрощёнными связями |

На мобильных изометрия нечитаема — для `sm` показать обычные карточки с иконками и стрелками между ними.

---

## ШАГ 5: Интеграция в страницу

### 5.1 Замена секции

В файле `src/pages/Home.jsx` (или аналогичном, где сейчас блок-схема лабораторий) заменить текущую визуализацию на:

```jsx
import IsometricEcosystem from '../components/IsometricEcosystem';

// В JSX:
<section id="ecosystem" className="min-h-screen py-20">
  <IsometricEcosystem />
</section>
```

### 5.2 НЕ удалять старые компоненты

Старую визуализацию переименовать в `EcosystemLegacy.jsx` — может понадобиться для сравнения или отката.

---

## ШАГ 6: Легенда и подписи

Под изометрической сценой — горизонтальная легенда потоков:

```
🔵 Данные/инструменты    🟡 Запросы/задачи    🟢 ROI/выручка
```

Реализовать как flex-row с цветными кружками и подписями. Без bullet points — inline элементы.

---

## ШАГ 7: Финальная полировка

### 7.1 Тени

Под каждым зданием — мягкая тень на "полу":

```css
.iso-shadow {
  filter: blur(20px);
  opacity: 0.3;
  background: rgba(0, 0, 0, 0.5);
  transform: rotateX(60deg) rotateZ(45deg) scale(1.1);
}
```

### 7.2 Фон секции

Фон — тёмный (`bg-slate-900`) с едва заметной изометрической сеткой (тонкие линии под углом 30°). Реализовать как CSS `background-image` с `repeating-linear-gradient`.

### 7.3 Заголовок секции

Над сценой — заголовок с Framer Motion анимацией:

```
Экосистема R&D лабораторий
Политех × Ростелеком
```

### 7.4 Плавающие метки ROI

Рядом с каждым зданием — "плавающая" метка с суммой выручки, пульсирующая анимация (`animate-pulse` или кастомная через Framer Motion). Цвет — зелёный. Формат: `₽5-15 млн/год`.

---

## ШАГ 8: Проверка

```bash
npm run build
npm run preview
```

### Чеклист:

- [ ] Три изометрических здания отображаются корректно
- [ ] Анимированные частицы двигаются между зданиями
- [ ] Hover на здание — подъём + детальная панель
- [ ] Управляющий комитет визуально "над" зданиями
- [ ] Участники от РТ и вуза видны по бокам
- [ ] ROI/выручка видна крупно и чётко (зелёным)
- [ ] Работает на десктопе (1920px, 1440px, 1024px)
- [ ] На мобильном (<768px) — fallback на карточки
- [ ] Нет ошибок в консоли
- [ ] Build проходит без warnings
- [ ] Анимация появления при скролле работает
- [ ] Framer Motion анимации плавные (60fps)
- [ ] Текст на зданиях читаемый (контраст ≥ 4.5:1)

---

## ШАГ 9: Коммит и push

```bash
git add -A
git commit -m "feat(viz): isometric 2.5D ecosystem visualization

- Add IsometricEcosystem component with SVG/CSS isometric buildings
- Animated data flows between labs (particles on paths)  
- Interactive hover with detailed panels and ROI metrics
- Responsive: isometric on desktop, cards on mobile
- Committee overlay and team participants sidebar"

git push origin feat/isometric-visualization
```

---

## Ограничения

- **НЕ добавлять** Three.js, React Three Fiber, или любые WebGL-библиотеки
- **НЕ добавлять** тяжёлые анимационные библиотеки (GSAP, anime.js) — Framer Motion достаточно
- **НЕ менять** структуру роутинга, Navbar, Footer
- **НЕ менять** данные других секций (Partners, About, etc.)
- Бандл не должен вырасти более чем на 5KB gzip сверх текущего
- Всё должно работать на GitHub Pages без серверной части

---

## Референсы для визуального стиля

Изометрический стиль как в:
- Infographics на Dribbble (запрос: "isometric infographic dark")
- Figma Community: "Isometric Illustration Kit"
- Ключевое: чистые линии, мягкие градиенты на гранях, subtle тени, анимация добавляет жизнь но не отвлекает

---

*Работать автономно. При сомнениях — SVG-подход приоритетнее CSS transforms. Главный критерий: руководитель компании должен за 5 секунд понять структуру и увидеть деньги.*
